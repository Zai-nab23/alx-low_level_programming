#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char b;

	char d;

	b = 'a';
	d = 'A';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	while
		(d <= 'Z') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
